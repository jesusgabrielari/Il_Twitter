<p id="notice"><%= notice %></p>

<h1>s</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @s.each do || %>
      <tr>
        <td><%= .content %></td>
        <td><%= .user %></td>
        <td><%= link_to 'Show',  %></td>
        <td><%= link_to 'Edit', edit__path() %></td>
        <td><%= link_to 'Destroy', , method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New ', new__path %>
